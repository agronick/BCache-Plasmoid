Simply run install.sh in a terminal to install.

If you make any modifications to the code you can just run install.sh. You do not need to uninstall anything first as install.sh does that prior to installation. 